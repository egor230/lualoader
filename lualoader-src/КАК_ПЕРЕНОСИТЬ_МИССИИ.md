# КАК ПЕРЕНОСИТЬ МИССИИ (памятка для ИИ)

Порядок и правила переноса миссии GTA Vice City с исходного SCM на Lua для
lualoader. Читать и соблюдать при каждой новой миссии.

**Дополнено 18.09.2026 (после 3-й сессии Riot, добавлены 9 C++-функций):**
раздел 2.2 — список добавленных функций и точные сигнатуры; раздел 5 — новые
приёмы (толпа с `wait_state`, розыскной блайп, выпадение оружия из трупа,
`set_ped_mood`, анимация RIOT); раздел 8 переписан под финальную версию
`misson riot.lua` (v3), использующую все 9 новых функций.

## 1. Где брать исходники

Папка «Source missions» внутри игры содержит ДВА набора:

- `Sanny Builder (мод, 34 миссии)/` — переработанные версии того же набора
  миссий (проще, урезанные).
- `Оригинал игры (97 миссий)/` — полные оригинальные миссии (опкод-декод от
  reVC, с катсценами и всеми деталями).

**ВАЖНО: по номеру/названию файла нельзя определять содержание.** Пример —
«06 - Riot»: в наборе «мод, 34 миссии» это миссия про репортёра Мэла и газ,
а в «Оригинал игры (97 миссий)» — настоящий «Мятеж»: рабочие взбунтовались
на стройплощадке, надо уничтожить грузовики (SET_RIOT_INTENSITY, толпа педов,
SPAND-грузовики). ВСЕГДА проверять содержимое по коду (CREATE_CAR/CREATE_CHAR/
текст/название потока), а не по имени файла.

## 2. Порядок работы

1. **Прочитать исходник целиком, построчно.** Разметить карандашом, где:
   подготовка (`GOSUB` init и катсцен), объекты сцены, **главный игровой цикл**
   (одна метка-цикл + `GOTO` в начало), условия успеха/провала, награда,
   очистка (этот блок в оригинале почти всегда есть — `MISSION_HAS_FINISHED`).
   В миссии Riot основным циклом является `:@L4725d` → `GOTO @L4725d`, а не
   `LOCATE`-цикл в шапке. Ищи строку вида `0002: GOTO @<та же метка>`.
2. **Выписать последовательность геймплея**: какие объекты, где стоят, что
   делает игрок, какие кнопки/блайпы наводят его на цель, когда миссия
   заканчивается, сколько денег и что ещё даёт награда.
3. **Выписать все исходные переменные**, которые меняются по ходу и влияют на
   поведение (флаги «уже сработало», счётчики). У каждой взять смысл:
   например `$2212` — «сколько пикетчиков игрок уже укусил», `$2198` —
   «сколько пикетчиков умерло», `$2213` — «переоделся ли», `$2205` — «бой
   начался». В Lua каждый такой флаг → отдельная `local`.
4. **Сопоставить каждый нужный опкод с Lua-функцией** (сверять с
   `LUA_API_СПРАВОЧНИК.md` и `lua_register` в plugin.cpp). Если функции нет —
   добавить в funcs.h (реализацию в стиле соседних: try/catch + writelog,
   русский комментарий, проверка типов через `lua_type`) + plugin.cpp
   (регистрацию), пересобрать `./build.sh`. Список уже добавленных для Riot —
   раздел 2.2.
5. **Написать миссию по каркасу** (раздел 3) и правилам (раздел 4). Локальные
   переменные внутри `if Star_mission_marker`, координаты инлайн, ни одного
   глобального состояния.
6. Проверить синтаксис: `luajit -bl "misson X.lua"` — без ошибок в конце.
7. Установить в папку `lualoader/` с кодировкой UTF-8 + BOM + CRLF
   (конвертировать скриптом, как в README). Только ОДНА миссия в папке
   одновременно (правило 5, раздел 4).
8. **Отдать на тест в игру.** Смотреть `lualoader/log.txt` (загрузились ли
   скрипты, нет ли `runtime`-ошибок) и `lualoader/trace.txt` (трассировка
   критичных функций: `SMM srabotal`, `pipir: …`, `createobj`, `Createcar: ok`).
   Прогнать до `miss(1000)` / `mission failed!`.

## 2.1 Сверка API: чего нет, и чем заменить (грабли переноса Riot)

Перед написанием — проверить КАЖДЫЙ вызов по `LUA_API_СПРАВОЧНИК.md` и
`lua_register` в plugin.cpp. Часть «очевидных» функций в lualoader отсутствует —
таблица проверенных замен:

| Опкод/механика оригинала | Нет в API | Рабочая замена |
|---|---|---|
| `HAS_PICKUP_BEEN_COLLECTED` | да | проверка расстояния `ped_in_point_in_radius(player, x, y, z, 2,2,2)` до точки пикапа |
| Катсцены (`LOAD_CUTSCENE`) | да | текст через `printmessage` + камера (`set_camera_position`/`camera_at_point`/`restore_camera`) + `fade` |
| `SET_RIOT_INTENSITY`, крики толпы | да (было) | теперь есть `set_riot_intensity(уровень)` (0552) — см. 2.2; крики — текстом `printmessage` |
| `ADD_SPRITE_BLIP_FOR_COORD` (спрайт) | частично | `radar_set_coord_blip(type,x,y,z,colour,display)` + `radar_set_blip_sprite(id, sprite)` — виден из любой точки карты |
| Одежда (`CREATE_CLOTHES_PICKUP`) | нет, есть лучше | `create_pickup_clothes(model, x, y, z)` + `set_skin(ped, "имя")` (модель одежды 2 = рабочий; `set_skin` в mod.lua подгружает скин) |
| Спец-акторы (`LOAD_SPECIAL_ACTOR`) | НО `Createped` умеет | `Createped("SPANDXA", x, y, z)` — спец-скин определится по `type_specpeds_list` (пол → базовый пед → скин); **не использовать** `create_spec_ped` (создаёт клона Томми) |
| Урон толпы (`SET_CHAR_WAIT_STATE`, `SET_CHAR_PERSONALITY`, `SET_CHAR_ONLY_DAMAGED_BY_PLAYER`, `IS_CHAR_HEALTH_GREATER`) | да (было) | теперь есть `set_ped_wait_state`, `set_ped_personality`, `set_ped_only_damaged_by_player`, `is_ped_health_greater` — см. 2.2 |
| Объекты-цели (`MAKE_OBJECT_TARGETTABLE`) | да (было) | теперь есть `make_obj_targettable(obj, flag)` — см. 2.2 |
| Выпавшее из трупа оружие (`GET_DEAD_CHAR_PICKUP_COORDS`) | да (было) | теперь есть `get_dead_char_pickup_coords(ped)` → x,y,z + `create_weapon_pickup` — см. 2.2 |
| Модели по id | — | только имена из списков funcs.h: `car_model_list` (MODEL_SPAND=213…), `name_peds_list` (GDA=91…), `type_specpeds_list` (IGGONZ, SPANDXA, …) |

**Порядок высадки/приёма:** опкоды `ENTER_CAR_AS_DRIVER/PASSENGER` и задачи
педа из машины — работают (`setcardrive`, `setcarpassenger`, `putincar`,
`exitcar`), но после `exitcar` пед ещё ~2 сек в анимации: давать задачу
`ped_walk_to_point` можно только после полного выхода (C++ теперь
принудительно высаживает через `warp_ped_out_of_car` — но цикл ожидания
`while is_ped_in_car(ped) do wait(100)` в миссии всё равно желателен).

## 2.2 Функции, добавленные для Riot (18.09.2026, стиль автора)

Все 9 — в funcs.h (прототипы ~стр. 294-302, реализации ~2810-2932) и
зарегистрированы в plugin.cpp (строки 363-371). Проверяют типы аргументов и
пишут ошибку в log.txt через writelog. Точные сигнатуры:

| Функция | Опкод | Сигнатура | Что делает | Проверено в funcs.h |
|---|---|---|---|---|
| `set_ped_wait_state` | 0372 | `(ped, state, timer)` | поставить педа в «ожидание» (у толпы Riot — 29, 99999999) | 3 числа, все обязательны |
| `set_ped_personality` | 0243 | `(ped, personality)` | тип личности (16 = обычный горожанин, 30 = агрессивный) | — |
| `set_ped_only_damaged_by_player` | 02A9 | `(ped, flag)` | педа может ранить только игрок (`true`/`false`) | 2-й аргумент `LUA_TBOOLEAN` |
| `is_ped_health_greater` | 0184 | `(ped, threshold) -> bool` | здоровье педа строго больше порога (для «ранить → в бой»: `not greater(99)`) | возвращает 1 значение |
| `make_obj_targettable` | 035D | `(obj, flag)` | в объект можно прицеливаться/бросать (булеан) | 2-й аргумент `LUA_TBOOLEAN` |
| `get_dead_char_pickup_coords` | 04A5 | `(ped) -> x, y, z` | координаты пикапа, что выпадает из трупа педа | возвращает 3 числа |
| `set_ped_mood` | 04E3 | `(mood, timer)` | настроение ИГРОКА (в Riot — `set_ped_mood(1, 60000)`, как `SET_PLAYER_MOOD $2 1 60000`) | 2 числа; НЕ пед, а игрок! |
| `request_animation` | 04ED | `(name)` | загрузить анимацию по имени (в Riot — `request_animation("RIOT")`) | строка |
| `remove_animation` | 04EF | `(name)` | выгрузить анимацию (`remove_animation("RIOT")` в очистке) | строка |

Уже были с прошлых сессий и тоже используются: `set_current_weapon_ped(ped,
тип)` (01B9 — положить в руки выданное оружие), `set_ped_stay_when_attacked
(ped, flag)` (0350 — не сходить с места при атаке), `set_riot_intensity(число)`
(0552), `ped_clean_threat(ped)` (01ED — очистить список угроз),
`ped_search_threat(ped, флаг)` (011A — добавить угрозу флагом:
1=игрок, 16=граждане), `clean_ped_wait(ped)` (053D — снять wait_state).
`set_riot_intensity` при этом — **свойство глобальное** (затронет и другие
миссии/игру), на выходе из миссии ставить 0.

## 3. Каркас миссии (шаблон) — МНОГОЭТАПНЫЙ

Большинство оригинальных миссий имеют несколько этапов:
**маркер начала → катсцена/диалог → пикапы/подготовка → поездка к цели →
сцена действия → условия победы.**

```
require("lualoader/mod")

function main()
while true do wait() local player = findplayer()

 -- ЭТАП 0: старт миссии у заказчика (например, офис Кена)
 if Star_mission_marker(9, X_START, Y_START, Z_START)
 then
  local step
  local mission_blip
  step = nil
  set_wanted(0)
  showtext(" Название", 500, 1)
  ma_trace("mission: СТАРТ у заказчика")

  -- Краткий текст вместо катсцены (LOAD_CUTSCENE нет в API)
  printmessage("~w~Ken: задание...", 5000, 1)

  -- Пикап одежды / подготовка (если есть в оригинале). Подбор проверяем
  -- РАССТОЯНИЕМ, потому что has_pickup_been_collected в API нет.
  local clothes = create_pickup_clothes(2, X_PICKUP, Y_PICKUP, Z_PICKUP)

  -- Глобальный блайп на цель (виден из любой точки карты)
  mission_blip = radar_set_coord_blip(0, X_TARGET, Y_TARGET, Z_TARGET, 2, 2)
  if mission_blip ~= nil then radar_set_blip_sprite(mission_blip, 19) end

  -- Предзагрузка анимаций в начале (request_animation), выгрузка в конце
  -- (remove_animation) — как в оригинале пара REQUEST_ANIMATION/REMOVE_ANIMATION.
  request_animation("RIOT")

  printmessage("~g~Поезжай к месту.", 5000, 1)
  step = 1

  while true == Getflagmission() do wait()

   -- ЭТАП 1: подъезд к цели (в машине/пешком)
   if step == 1 and ped_in_point_in_radius(player, X_TARGET, Y_TARGET, Z_TARGET, RX, RY, RZ)
   then
    if mission_blip ~= nil then remove_blip(mission_blip) mission_blip = nil end
    -- Создаём сцену в темноте (SMM уже затемнил экран)
    set_riot_intensity(127)
    ... Createcar/Createped/create_marker на месте действия ...
    fade(1, 1200) wait(1200)          -- засветка
    printmessage("~g~Цель на месте: уничтожить грузовики.", 6000, 1)
    step = 2
   end

   -- ЭТАП 2: игровой процесс (убийство целей, защита и т.п.)
   if step == 2 and условие_победы
   then
    ... чистка бланков/машин/педов ...
    set_riot_intensity(0)
    remove_animation("RIOT")
    ma_trace("mission: ВЫПОЛНЕНА")
    miss(НАГРАДА)
    step = nil
    break
   end

  end
  step = nil
 end
end
end
```

- **Внешний цикл** — ждёт подъезда к значку НА МАШИНЕ. `Star_mission_marker`
  создаёт значок, высаживает игрока, убирает его авто, поднимает флаг миссии.
- **Внутренний цикл** — `while true == Getflagmission()` пока миссия активна.
  Каждый этап = блок `if step == N`. Успех — `miss(деньги)`, провал —
  `end_mission("mission failed!")`.
- Смерть/арест игрока обрабатывает C++-сторож `Getflagmission` (функция
  `destroy()` очищает всё созданное).
- **Флаги-«одноразы»** из оригинала (например «бой начался», «уже показал
  текст») — `local` и обнулять сразу после выполнения условия, чтобы не
  срабатывали дважды.

## 4. Правила переноса

1. **Координаты — инлайн.** НЕ выносить в верхний блок констант вида
   `local X, Y, Z = ...` с шапкой «-- Координаты из оригинальной миссии».
   Такой блок только засоряет код. Писать координаты прямо в месте
   использования (в вызове Createcar/Createped/ped_in_point_in_radius и т.п.).
2. **Все переменные состояния — local** (объявлять в начале блока after
   `if Star_mission_marker`). Все скрипты папки lualoader/ крутятся в потоках
   на ОБЩЕМ lua_State, поэтому глобалы разных миссий (step, blip, car...)
   пересекаются и ломают друг друга.
3. **Катсцены и спец-скины не переносятся буквально.** Полноценные катсцены
   (LOAD_CUTSCENE LAW_*) нет API — заменять коротким текстом через
   printmessage. Спец-модели (LOAD_SPECIAL_CHARACTER) не грузятся — брать
   обычных педов из name_peds_list / спец-акторов через `Createped("SPANDXA")`.
   Эффекты (SET_RIOT_INTENSITY) — теперь ЕСТЬ (`set_riot_intensity`), включать
   как в оригинале, в конце миссии выкл.
4. **Модели - только по именам из списков funcs.h:**
   - Createcar("MODEL_<Название>", x, y, z) — имена в `car_model_list`
     (напр. MODEL_SPAND=213, MODEL_PONY=143).
   - Createped("<Имя>", x, y, z) — имена в `name_peds_list` (обычные педы,
     напр. HMYST, GDA) или спец-акторы в `type_specpeds_list` (IGGONZ и т.п.).
- Оружие Giveweaponped(ped, патроны, "имя") — имена в `name_weapon_list`/
      `types_weapon_list` (molotov=тип 15, colt45=тип 17, nitestick=тип 4).
   Сверять с data/default.ide (id → имя), вызывать по имени из списков.
5. **Одна миссия в папке lualoader одновременно.** star_mission_marker в C++
   использует общие static (point/create) — две миссии будут драться за
   значок. Остальные скрипты (autoaim и диагностика) не мешают.
6. Кодировка Lua-файла: UTF-8 с BOM и CRLF. MD-документы: UTF-8 без BOM, LF.
7. Каждое имя функции проверять: оно должно быть зарегистрировано
   (`lua_register` в plugin.cpp) либо определено в mod.lua. Иначе — runtime
   ошибка в игре.

## 5. Подводные камни платформы

- `ped_frozen(0)` = управление игроком ВЫКЛЮЧЕНО (заморожен), `ped_frozen(1)` —
  включено. Именование обратное смыслу.
- `end_mission()` вызывает destroy() — чистит ВСЁ созданное миссией; свои
  переменные-указатели при этом обесцениваются — обнулять и не использовать.
- `remove_fire(id)` стирает запись из внутренней map (иначе destroy() удалит
  огонь повторно). Все remove_* гигиеничны.
- `set_current_weapon_ped(ped, тип)` — положить оружие педу в руки (01B9).
  Сначала Giveweaponped (выдать + загрузить модель), потом эта функция.
- `set_ped_stay_when_attacked(ped, true)` — пед не сходит с места при атаке
  (0350), как «бригадир» с кольтом в Riot.
- `ped_in_point_in_radius(ped, x,y,z, rx,ry,rz)` — чистое расстояние по 3D,
  работает и в машине, и пешком (внутри sleep ~10мс — не злоупотреблять
  количеством вызовов в тик).
- `iscardead(car)` — авто уничтожено (для условий «уничтожь все грузовики»).
- `getpedhealth(victim) == 0` — пед мёртв; sethealth(e,n) добавляет +0.99.
- **Толпа с wait_state (бой, как в Riot):** педы создать с
  `set_ped_wait_state(ped, 29, 99999999)` + `ped_clean_threat` +
  `set_ped_personality(ped, 16)` + `set_ped_only_damaged_by_player(ped, true)`.
  Они стоят, пока игрок не ранит: `is_ped_health_greater(ped, 99) == false`
  → снять wait (`clean_ped_wait`) → `set_char_obj_kill_player_on_foot(ped,
  player)`. «Рассердить всех сразу» — `set_ped_personality(ped, 30)` +
  `ped_search_threat(ped, 16)` + `ped_search_threat(ped, 1)` каждому живому.
- **Выпадение оружия из трупа:** `get_dead_char_pickup_coords(ped)` вернёт
  x,y,z (про Z уже прибавить 0.5, как в оригинале `$2250 += 0.5`), затем
  `create_weapon_pickup(274, 3, 17, x, y, z)` (274 = модель colt45,
  патроны/тип 17). Так в оригинале из убитых зачинщиков падают кольты.
- **`set_ped_mood` действует на ИГРОКА, а не на педа** (обёртка 04E3
  SET_PLAYER_MOOD) — не путать с настроением толпы.
- Настроение игрока в миссии Riot ставится один раз после катсцены —
  `set_ped_mood(1, 60000)`, ровно как в оригинале.

## 6. Типичные ошибки (на примере Riot / Мятежа)

Три итерации: 1-я — маркер на стройплощадке, HMYST вместо рабочих, замена
толпы; 2-я (v2) — перенесено, но 9 опкодов заменялись «текстом» (не было
API); 3-я (v3) — добавлены 9 C++-функций, поведение толпы и выпадение
оружия стали как в оригинале.

| Ошибка | Что было в оригинале | Как сделать правильно |
|--------|----------------------|----------------------|
| **Старт не там** | Игрок получает миссию в офисе Кена Розенберга (95.4, -1135.5, 9.4) | SMM-маркер ставить **у заказчика**, а не на месте действия |
| **Нет пикапа одежды** | `CREATE_CLOTHES_PICKUP 97.5 -1133.6 10.4 2` — переодевание в рабочего | `create_pickup_clothes(2, 97.5, -1133.6, 10.4)` + `set_skin(player, "SPANDXA")` + проверка подбора **расстоянием** |
| **Маркер не виден глобально** | Два блайпа со спрайтами (офис + стройка) — на карте из любой точки | `radar_set_coord_blip(0, 299.0, -314.6, 11.4, 2, 2)` + `radar_set_blip_sprite(id, спрайт)` — display=2 (карта+радар) |
| **Не те педы** | Спец-скины SPANDXA/SPANDXB (модели 112/113) — рабочие в спецодежде | `Createped("SPANDXA"/"SPANDXB", x, y, z)` — чередовать; `Createped` их **поддерживает** (через `type_specpeds_list`) |
| **Нет атмосферы бунта** | `SET_RIOT_INTENSITY` 127, крики протеста | `set_riot_intensity(127)` + серии `printmessage` с текстом протеста |
| **Толпа не дерётся как в оригинале** | wait_state 29, personality 16, только игрок может ранить; при укусе — идти убивать игрока | `set_ped_wait_state`/`set_ped_personality`/`set_ped_only_damaged_by_player` + `is_ped_health_greater` в цикле (см. 5) |
| **Зачинщики не роняют оружие** | `GET_DEAD_CHAR_PICKUP_COORDS` + `CREATE_PICKUP_WITH_AMMO 274 3 17` | `get_dead_char_pickup_coords` + `create_weapon_pickup(274, 3, 17, …)` один раз на труп (флаг) |
| **Пропущен этап подготовки** | Кен → пикап одежды → поездка → стройплощадка | Каркас: step=0 (у заказчика) → пикап одежды → глобальный блайп → step=1 (подъезд, радиус ~70) → step=2 (сцена в темноте) → step=3 (бой/уничтожение) |
| **Глобалы в миссии** | — | все переменные `local` (скрипты на общем `lua_State` — `step`/`blip`/`car` пересекутся с другими миссиями) |

## 7. Чек-лист перед установкой миссии

- [ ] Прочитан **оригинальный** SCM (не мод-версия), выписаны все этапы и
      главный цикл (метка, на которую `GOTO` в конце тела).
- [ ] Стартовый маркер — там, где игрок получает задание (у заказчика).
- [ ] Все этапы: диалог/текст → пикапы/подготовка → глобальный блайп на цель → сцена.
- [ ] **Каждый вызов сверен с API** (раздел 2.1/2.2): отсутствующие функции
      заменены по таблице либо добавлены в C++ (стиль автора) и пересобрано.
- [ ] Флаги из оригинала («уже сработало») перенесены отдельными `local` и
      обнуляются после срабатывания.
- [ ] Координаты **инлайн** в местах использования, без верхнего блока констант.
- [ ] Все переменные состояния объявлены `local` внутри `if Star_mission_marker`.
- [ ] Модели авто/педов/оружия — только по именам из `funcs.h` (car_model_list,
      name_peds_list, type_specpeds_list, types_weapon_list); спец-акторы —
      через `Createped` (не `create_spec_ped`).
- [ ] Глобальный блайп на цель (`radar_set_coord_blip` + `radar_set_blip_sprite`)
      виден из любой точки карты.
- [ ] Загруженные анимации выгружены (`request_animation` ↔ `remove_animation`);
      `set_riot_intensity(0)` на выходе из миссии.
- [ ] В папке `lualoader/` — ТОЛЬКО эта миссия (другие перенесены/удалены).
- [ ] Синтаксис проверен: `luajit -bl "misson X.lua"` → OK.
- [ ] Файл сохранён UTF-8 BOM + CRLF в папке `lualoader/`.
- [ ] `./build.sh` пересобран, если добавлены новые C++ функции.

## 8. Пример правильного переноса: «misson riot.lua» (Мятеж), версия v3

Полная версия (18.09.2026, использует все 9 функций из 2.2; `luajit -bl` OK,
установлена в `lualoader/`). Логика — по оригиналу LAWYER4:

1. **Этап 0** — SMM **у офиса Кена** (95.4, -1135.5, 9.4): текст-«катсцена»
   (Лоув + Авери), `set_ped_mood(1, 60000)`; пикап одежды
   `create_pickup_clothes(2, 97.5, -1133.6, 10.4)` с проверкой подбора
   **расстоянием**; `request_animation("RIOT")`; **два глобальных блайпа**
   (`radar_set_coord_blip` + `radar_set_blip_sprite`): офис (95.4,-1135.5,9.4,
   спрайт 28) и стройка (299,-314.6,11.4, спрайт 19).
2. **Этап 1** — подъезд к стройплощадке (`ped_in_point_in_radius`, радиус 70):
   убрать оба блайпа; `set_riot_intensity(127)`; в темноте создать сцену:
   - 3 грузовика `Createcar("MODEL_SPAND", …)` (286.7,-302.1; 312.2,-304.7;
     312.2,-300.7; высота 11.9, углы 270/90/90) + маркеры `create_marker(truck)`;
   - **21** рабочий `Createped("SPANDXA"/"SPANDXB", …)` (чередованием, как
     оригинал 112/113) ровно по координатам оригинала, каждому:
     `ped_clean_threat` + `set_ped_personality(16)` +
     `set_ped_only_damaged_by_player(true)` + `set_ped_wait_state(29, 99999999)`;
- 3 зачинщика `Createped("GDA", …)` (301.2,-311.5; 303.2,-311.5 — с
      `nitestick` (тип 4, GIVE_WEAPON 4 0), лидер 304.0,-298.7 — `colt45`
      (тип 17) 300 патронов, HP 70, `set_ped_stay_when_attacked`), все
      `set_ped_only_damaged_by_player(true)`;
   - 2 объекта `createobj(-114, 309.3,-298.6,10.9)` и
     `createobj(-114, 284.3,-312.4,10.9)` + `make_obj_targettable(obj, true)`;
   - игроку — коктейли Молотова (`Giveweaponped(…, "molotov")`) + в руки.
   `fade(1, 1200)`, тексты протеста.
3. **Этап 2** — БОЙ (главный цикл, как L4725d оригинала): пока все 3 грузовика
   живы — если пикетчик жив и `is_ped_health_greater(ped, 99) == false` и его
   флаг «укушен» ещё не стоял → `clean_ped_wait` + `set_char_obj_kill_player_
   on_foot`, флаг=1; если пикетчик мёртв → `set_riot_intensity(max(...,0))`
   (убывает по мере убийств). Если зачинщик убит и ещё не падал пикап →
   `get_dead_char_pickup_coords` → `create_weapon_pickup(274, 3, 17, …)`
   (ид разок). Триггер «всех в бой» — число укушенных > 3 или убитых > 2 →
   живым: `set_ped_personality(30)` + `ped_search_threat(16)` +
   `ped_search_threat(1)`, лидеру власть + точность.
4. **Этап 3** — все 3 грузовика уничтожены (`iscardead`) → чистка
   (`remove_blip(marker)`/`remove_ped`/`remove_car`/`remove_obj`),
   `set_riot_intensity(0)`, `remove_animation("RIOT")`, `miss(1000)`.
5. **Провал** — гибель игрока/арест — C++-сторож `Getflagmission`
   (см. `ЗАМЕТКУ_О_МИССИЯХ.md`); агент/сопровождаемый проверяется отдельно,
   если он есть в миссии.

Грузовиков в оригинале **три** (SPAND, модель 213); пикетчиков **21** (не 8);
зачинщиков **3** (2 с дубинами + 1 «бригадир» с кольтом и HP 70).

---
*Документ обновлён 18.09.2026: добавлены раздел 2.2 (9 новых C++-функций с
сигнатурами), приёмы толпы и выпадения оружия (раздел 5), раздел 8 переписан
под версию v3.*