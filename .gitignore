# Исключения: кэш/мусор сборки, бэкапы, сторонние исходники Lua, локальный лог
lualoader-src/obj/
lualoader-src/lua/
lualoader-src/*.bak12
lualoader-src/*.bak13
lualoader/log.txt
lualoader/lualoader.tar

# Этот скрипт (чтобы не отправлять токен на GitHub)
/updata git.sh
